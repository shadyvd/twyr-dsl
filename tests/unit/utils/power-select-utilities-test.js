import powerSelectUtilities from 'dummy/utils/power-select-utilities';
import { module, test } from 'qunit';

module('Unit | Utility | power-select-utilities', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = powerSelectUtilities();
    assert.ok(result);
  });
});
