import calculateDropdownPosition from 'dummy/utils/calculate-dropdown-position';
import { module, test } from 'qunit';

module('Unit | Utility | calculate-dropdown-position', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = calculateDropdownPosition();
    assert.ok(result);
  });
});
