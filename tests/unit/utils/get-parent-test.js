import getParent from 'dummy/utils/get-parent';
import { module, test } from 'qunit';

module('Unit | Utility | get-parent', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = getParent();
    assert.ok(result);
  });
});
